package game.shared.constants

/** Shared tuning values for server-authoritative entity visibility. */
object InterestManagementConstants {
    /** Maximum distance, in world units/meters, at which another entity is replicated to a player. */
    const val VISIBILITY_RADIUS_WORLD_UNITS: Float = 16f
}
